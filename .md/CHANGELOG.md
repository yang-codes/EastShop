# EastShop 变更记录

## 2026-06-02

- `submit-order` 增加下单安全防护：隐藏 honeypot、IP 级限流、手机号级限流，以及 5 分钟重复订单检测。
- 重复订单检测按手机号、来源、金额和商品/规格/数量指纹判断；Edge Function 会先查近期候选订单，订单写入时保存 `orders.cart_fingerprint`。
- 新增 Supabase migration：`013_order_duplicate_guard.sql`，为订单表增加 `cart_fingerprint`，并创建 `prevent_recent_duplicate_order` 数据库触发器，在插入层阻止 5 分钟内重复订单；重复时返回 `409 DUPLICATE_ORDER`。
- 线上生效需要同时执行数据库迁移并重新部署 `submit-order` Edge Function；只更新前端不会触发该保护。
- 下单页定位链路调整为 Telegram Mini App 优先调用 `Telegram.WebApp.LocationManager`，失败后回退浏览器 Geolocation API；Web 和 Instagram 入口继续使用浏览器原生定位，经纬度统一交由 `reverse-geocode` 反查地址。
- 来源识别增加 Instagram 自动识别和当前会话记忆：Instagram 内置浏览器或 `instagram.com` referrer 会自动标记为 Instagram，站内跳转不需要每个链接都手动带 `source=instagram`。
- 下单页地址反查改为通过 `reverse-geocode` Supabase Edge Function 调用 Geoapify，前端不再读取 `VITE_GEOAPIFY_API_KEY`，Geoapify key 改存 Supabase secret `GEOAPIFY_API_KEY`。
- 未配置 Supabase 的本地开发/mock 模式下，后台路由会使用本地预览管理员身份进入管理页，便于直接验证后台商品、订单、分类和店铺配置页面。
- 下单页定位在非安全来源被浏览器拦截时，改为显示明确的 HTTPS/localhost 提示，避免直接暴露浏览器英文错误。
- 分类管理和店铺配置接入翻译补齐：分类名称、手机号前缀名称可从中文补齐英文/俄文，保存时空白 EN/RU 会使用中文兜底。
- 店铺配置手机号前缀翻译补齐改为每条前缀单独触发，避免全局一次性补齐。
- 优化后台店铺配置页手机号前缀卡片排版：标题区显示摘要，字段改为多列分组，补齐和删除操作固定在卡片右侧。
- 店铺配置点击“新增前缀”后自动滚动到新增卡片，并聚焦中文名称输入框。
- Web 商品详情页左侧商品切换优化为先使用已加载商品数据即时刷新右侧详情，再同步更新 URL；重复点击当前商品不会重复触发跳转。
- 新增后台“店铺配置”页面 `/admin/store-settings`，支持管理员维护提交订单和我的订单查询页使用的手机号国家/地区前缀下拉选项。
- 新增 Supabase migration：`009_store_settings.sql`，创建公开可读、仅管理员可写的 `store_settings` 单例配置表，保存手机号前缀配置。
- 店铺配置新增社媒平台来源配置，默认 Telegram、Instagram、Facebook 和其他；下单页将社交账号改为平台来源下拉 + 账号输入。
- 新增 Supabase migration：`011_social_platform_settings.sql`，为 `store_settings` 增加 `social_platforms`，为 `orders` 增加 `social_platform`；订单后台、订单查询和飞书通知会展示平台来源。
- 结算成功页移除页头重复的“继续浏览商品”入口，仅保留成功提示卡片内的主要入口。
- 前台新增“我的订单”入口和 `/orders` 查询页，支持手机号 + 订单号或手机号 + 社交账号查询。
- 新增 `lookup-orders` Supabase Edge Function，公开查询入口仍要求手机号参与校验，并返回订单状态、商品/规格、数量、金额、地址和下单时间；后台改 `orders.status` 后前台查询会实时读取最新状态。
- `service_role` 订单权限补充 `order_items select`，用于订单自助查询返回订单明细。
- 新增商品销售规格能力：后台商品编辑支持新增、删除、启用、设置默认规格，并为每个规格配置三语名称、价格、SKU 和排序。
- 前台商品详情页支持选择销售规格，价格随规格切换；购物车按商品 + 规格拆分商品行，结算页、订单明细和飞书订单通知都会带上规格名称。
- 新增 Supabase migration：`008_product_variants.sql`，为 `products` 增加 `variants` JSON 字段，为 `order_items` 增加 `variant_id` 和 `variant_name` 快照字段，并为旧商品生成默认规格。
- 新增后台通知配置页面 `/admin/notifications` 和侧边栏“通知配置”入口，支持保存飞书订单通知启用状态、机器人 webhook、签名密钥，并提供测试发送按钮。
- 新增 Supabase migration：`007_notification_settings.sql`，创建 `notification_settings` 单例配置表、管理员 RLS、表权限和 `service_role` 读取权限。
- `submit-order` Edge Function 在订单写入成功后读取 `notification_settings`，启用飞书时发送订单提醒；飞书发送失败仅记录日志，不影响订单提交成功。
- 飞书机器人签名逻辑按自定义机器人安全设置生成 `timestamp` 和 `sign`，并检查 webhook 响应码，便于排查配置错误。
- `submit-order` 读取通知配置时增加错误日志，避免 `notification_settings` 权限或迁移缺失时静默跳过飞书通知。
- 飞书订单通知内容增加订单来源展示，可区分 Web、Telegram 小程序和 Instagram。
- 后台商品编辑改为中文字段为主：英文/俄文字段可留空，保存时自动用中文兜底，并新增“补齐 EN/RU”按钮用于补齐空白语言字段。
- 后台商品图片 URL 列表调整为只读展示，图片列表只能通过缩略图操作修改：Supabase Storage 图片显示“删除文件”，本地或外部图片显示“移除引用”。
- 后台商品主图和详情图缩略图支持拖拽排序，拖动后同步更新只读 URL 列表，保存商品后写入新的图片顺序。
- 商品详情页按终端优化其他商品列表：桌面 Web 保留左侧商品切换，小程序来源和手机端详情页隐藏其他商品，只展示当前商品详情。
- 手机端商品详情页参考主流电商详情页节奏优化：头图优先展示，价格和商品信息紧跟头图，规格信息卡片化，加入购物车按钮固定在底部安全区上方。
- 结算页“电话”字段改为“手机号”，新增国家/地区电话前缀下拉框，提交订单时保存完整国际手机号。
- 手机号前缀下拉框新增俄罗斯 `+7` 和“其他”选项；选择“其他”时可手动填写国际电话前缀。
- 结算页必填规则调整为仅手机号必填，姓名和详细地址改为可选；飞书通知中空姓名和空地址不再显示对应行。
- 飞书订单通知增加备注展示：用户填写备注时通知内容会追加“备注”行。
- `submit-order` 增加 Telegram Mini App `initData` 服务端验签：使用 `TELEGRAM_BOT_TOKEN` 校验 `hash` 和 `auth_date`，并将验签后的 Telegram 用户快照写入订单。
- Telegram Mini App 订单新增已验签用户 ID 持久化；“我的订单”页在 Telegram 环境自动通过 `initData` 查询本人订单，无需输入手机号、订单号或社交账号。
- 手机端商品详情页修正顶部操作区和内容卡片对齐：导航按钮在窄屏下保持同一行，头图、购买卡片和详情图片区统一左右边距，规格按钮在手机端撑满可用宽度。
- 前台顶部导航新增主页图标入口；“我的订单”页移除页内“继续选购”按钮，减少移动端重复入口和表单上方占位。
- 商城首页移动端筛选栏调整为组合搜索控件：分类下拉嵌入搜索框左侧，“推荐”开关嵌入搜索框右侧，统一控件高度和焦点样式。
- 前台手机端顶部导航优化：隐藏来源标签，购物车固定在品牌右侧，语言切换折叠为当前语言按钮并通过弹层选择其他语言。
- 商城首页手机端商品列表改为两列展示，并压缩卡片间距、标题和简介字号，提升移动端商品浏览密度。
- 购物车手机端商品卡片优化：图片固定在左侧独立区域，商品文字、规格和金额在右侧换行展示，避免文字覆盖产品图。
- 商品详情页移除开发占位提示：商品不存在、已下架或链接失效时显示正式空状态，并提供返回主页按钮。
- `translate-text` Supabase Edge Function 改为通过服务端代理腾讯云机器翻译补齐英文和俄文，避免腾讯云 SecretKey 暴露在前端。
- 后台商品“补齐 EN/RU”改为串行限速调用，`translate-text` Edge Function 增加腾讯云 `RequestLimitExceeded` 重试和 EN/RU 顺序翻译，避免超过 5 次/秒限制。
- 后台商品翻译补齐改为按模块执行：名称、简介、详情、规格属性、销售规格分别触发，只提交当前模块的中文内容进行 EN/RU 补齐。
- 后台商品管理搜索区改为一体化组合控件，分类、商品搜索和推荐筛选放在同一个圆角搜索条中，并支持按推荐商品过滤列表。
- 后台商品管理一体化搜索条改为撑满工具栏整行，中间商品搜索框自动占用剩余宽度。
- 后台商品管理一体化搜索条新增上下架状态筛选，支持按全部状态、上架、下架过滤左侧商品列表。
- 手机端购物车商品卡片调整删除按钮位置，与数量步进器同处一行并靠右展示，避免单独掉到下一行。
- 手机端购物车合计卡片改为固定在屏幕底部，不随商品列表滚动，并为列表增加底部留白避免遮挡内容。
- 手机端购物车合计卡片优化为底部结算条样式，合计金额与下单按钮并排展示，降低遮挡面积并提升操作层级。
- 手机端前台顶部隐藏独立品牌标题，将主页、我的订单、语言和购物车入口压缩到首行展示，减少头部占用高度。
- 手机端购物车商品图文区域支持点击进入对应商品详情页，数量调整和删除按钮保持独立操作。
- 提交订单成功后，标题区新增“已收到订单，我们会尽快与你取得联系”提示，明确后续联系预期。
- 提交订单成功后的“已收到订单，我们会尽快与你取得联系”提示改为红色加粗显示。
- 商品详情页增加加载态和旧链接兼容，旧 slug 链接会按商品多语言名称快速匹配并替换跳转到真实商品 ID，避免先进入“商品不可访问”中转页。
- 我的订单移动端查询结果增强订单卡片分隔、头部背景和明细卡片样式，让多笔订单之间更容易区分。
- “我的订单”查询结果支持用户取消新订单；只有 `new` 状态展示取消按钮，取消成功后状态更新为已取消。
- 新增 `cancel-order` Supabase Edge Function，服务端按手机号和订单号校验订单，只有新订单可取消，并在取消成功后发送飞书取消通知。
- 新增 Supabase migration：`010_order_cancel_permissions.sql`，补齐 `service_role` 取消订单所需的 `orders update`、`order_items select` 和通知配置读取权限。
- 已在 Supabase secrets 配置腾讯云机器翻译所需密钥项：`TENCENTCLOUD_SECRET_ID`、`TENCENTCLOUD_SECRET_KEY`、`TENCENT_TRANSLATE_REGION`；更新函数后需重新部署 `translate-text`。
- 后台商品、分类、店铺配置和通知配置保存成功后自动回到页面顶部，方便查看保存结果提示。

## 2026-05-30

### 已完成或已有骨架
- 项目已初始化为 React + Vite + TypeScript。
- 已建立前台路由：`/`、`/product/:productId`、`/cart`、`/checkout`。
- 已建立后台路由：`/admin/login`、`/admin/products`、`/admin/orders`、`/admin/categories`。
- 商品列表页可从 Supabase 或 mock JSON 加载已上架商品。
- 商品详情页可展示三语商品名称、简介、详情、规格、标签和主图。
- 购物车已使用 `localStorage` 持久化，支持加减数量、删除、合计金额。
- 下单页已有客户字段、浏览器定位按钮和 Google Maps 链接。
- 多语言框架已接入 `react-i18next`，当前界面文案支持中文、英文、俄文三语。
- 商品列表页新增搜索、分类筛选和只看推荐筛选。
- 商品详情页新增多图展示，当前详情图以一张占一行的纵向详情图流展示。
- 下单页改为受控表单，增加姓名、电话、地址基础校验。
- 下单页读取购物车并展示订单摘要、明细和合计。
- 下单成功后清空购物车，并展示订单号。
- 未配置 Supabase 时，下单返回本地预览订单号，便于 mock 模式跑通前台闭环。
- 配置 Supabase 后，`orderService.submitOrder` 调用 `submit-order` Edge Function。
- 来源识别支持普通 query 和 HashRouter hash query 中的 `source=telegram|instagram|web`，并支持 Instagram 内置浏览器/referrer 自动识别和当前会话记忆。

### 后台管理
- 商品管理页增加 KPI、搜索、分类筛选、商品列表和商品编辑表单骨架。
- 订单管理页增加状态统计、搜索、状态筛选、订单卡片和导出入口。
- 分类管理页增加搜索、分类列表和分类编辑表单骨架。
- `adminOrderService.listOrders` 增加开发态 mock 订单，便于后台订单页在未接 Supabase 前展示。
- 商品管理接入新增、编辑、删除、上下架、推荐和排序保存。
- 分类管理接入新增、编辑、删除、排序和启用状态保存。
- 订单管理接入状态更新。
- `adminProductService` 增加商品和分类的 Supabase 映射、upsert 和 delete 方法。
- `adminOrderService` 增加 Supabase 订单读取和订单状态更新。
- 后台登录页接入 Supabase Auth 邮箱密码登录。
- 后台路由接入 `admin_profiles` 授权检查，未登录或未授权用户跳转登录页。
- 退出按钮接入 Supabase Auth 登出。
- 修正 `admin_profiles` snake_case 到前端 `AdminProfile` camelCase 的映射。
- 修复登录页对象错误直接显示为 `[对象 对象]` 的问题，改为提取 `message` 或显示通用登录失败提示。
- 定位后台登录后查询 `admin_profiles` 返回 403 的原因：表基础访问权限缺失或管理员授权行/RLS 未配置完整。
- 后台商品管理“管理图片”按钮接入文件选择、浏览器端 WebP 压缩、Supabase Storage 上传，并自动回填主图 URL 和图片 URL 列表。
- 后台商品图片字段拆分为主图和详情页图片：主图上传只回填主图 URL，详情页图片列表单独上传；前台详情页以纵向详情图流展示详情页图片列表。
- 修复商品详情页详情图较长时右侧信息卡被拉伸，导致标签和加入购物车按钮纵向变形的问题。
- 商品详情页增加主图角标和详情图片分区标题，明确区分主图与详情图。
- Web 商品详情页改为左侧商品列表、右侧商品详情的联动布局，点击左侧商品切换当前详情并更新深度链接；商城主页保留商品宫格。
- 优化 Web 商品详情页样式：左侧商品清单改为紧凑可扫读列表，右侧详情区强化商品主图、购买卡和详情图片区分，购买卡在桌面端保持吸顶。
- 购物车页标题区、空购物车状态和合计卡增加“继续选购”入口，方便用户返回商城继续加购。
- 商品主图升级为多图：新增 `005_product_cover_images.sql` 迁移，后台主图支持多选上传和多行 URL 管理，前台详情页单张主图静态展示，多张主图自动轮播展示。
- 后台商品图片字段增加尺寸建议：主图标注推荐 4:3、至少 1200 x 900 px；详情图标注推荐宽度至少 1200 px。
- 修复商品详情页桌面布局中右侧购买卡可能压住主图的问题，详情区图片列和购买卡列现在会在可用宽度内稳定分栏。
- 修复后台商品保存、上传、删除等失败提示直接显示 `[object Object]` 的问题，现在会展开 Supabase 错误的 message、details、hint 和 code。
- `005_product_cover_images.sql` 增加 PostgREST schema cache reload 通知，执行新增 `cover_images` 字段迁移后可避免保存商品时继续命中旧 schema。
- 后台商品主图和详情图上传完成后增加成功弹窗提示，同时保留页面内状态提示。
- Web 商品详情页购买卡在桌面端右对齐并增加左侧安全间距，避免贴近或遮挡主图。
- Web 商品详情页购买卡在桌面端改为固定在视口右侧，页面上下滚动时保持可见；移动端仍为正常单列布局。
- 修正固定购买卡与详情主图重叠的问题：宽桌面下为右侧固定卡预留内容避让区，窄屏回退为普通布局。
- Web 商品详情页固定购买卡改用商城页 `1540px` 宽度基准定位，并提高固定布局启用断点，避免中等屏幕下与主图重叠。
- 提交订单页标题栏增加“继续选购”入口，用户在填写订单时也可以返回商城继续添加商品。
- 去除购物车页标题栏重复的“继续选购”入口，购物车有商品时保留合计卡中的继续选购按钮。
- 后台商品编辑新增规格属性编辑器，支持新增、删除和编辑三语属性名/属性值，并保存到商品 `specs` 字段供前台详情购买卡展示。
- 后台商品编辑的主图 URL 列表调整为全宽字段，提升多 URL 编辑时的可读性。
- 后台商品编辑的详情图上传按钮移到详情页图片 URL 列表右侧，与主图上传布局保持一致。
- 后台商品和分类编辑页底部的删除、保存按钮改为居中显示。
- 后台商品编辑页底部操作按钮放大，并调整为保存按钮在左、删除商品按钮在右。
- 后台商品编辑页的上架、推荐勾选项改为整行状态控件，优化文字和勾选框的对齐。
- 后台商品编辑页的上架和推荐状态项调整为同一行展示，保持文字在左、勾选框在右。
- 修正后台表单通用 label 样式覆盖勾选项的问题，确保上架、推荐等状态项按左右布局显示。
- 后台勾选状态项的文字和勾选框改为在控件内居中显示。
- 后台管理页面增加专属浅色背景、侧栏分层、工具栏底色和卡片阴影，提升页面层次感。
- 后台商品列表新增上下架状态徽标，上架商品显示绿色底小图标和“上架”文字，下架商品显示低强调状态。
- 文档补充后台“通知配置”菜单规划，明确飞书订单通知由后台配置并由 `submit-order` Edge Function 在订单写入成功后发送。
- 新增 `submit-order` Supabase Edge Function，实现 Web/Instagram 订单服务端校验、写入订单和订单明细，并预留飞书通知读取配置后的发送逻辑。
- 前台订单提交改为直接解析 `submit-order` Edge Function 响应体，错误时显示后端返回的具体 message，便于定位 500 问题。
- 将 `submit-order` 配置为公开 Edge Function，并调整前端请求只发送 `apikey`，避免 Supabase publishable key 被当作 JWT 导致下单返回“无效的 JWT”。
- `submit-order` 增强数据库错误诊断，商品查询、订单写入和订单明细写入失败时返回明确错误码和 Supabase 详情，前台保留非 JSON 响应文本用于排查。
- 结算页提交订单时只发送当前商品列表可匹配到的购物车项，并自动清理本地购物车中的失效商品 ID，避免旧缓存商品导致 `PRODUCT_NOT_FOUND`。
- 结算页订单摘要合计改为每次渲染直接汇总当前明细小计，并增加 `data-total` 便于排查缓存或 DOM 显示问题。
- 下单页定位接入 Geoapify Reverse Geocoding；当前已迁移为通过 `reverse-geocode` Edge Function 读取服务端 `GEOAPIFY_API_KEY`，把浏览器经纬度转换为国家、城市、街道和格式化地址，并回填详细地址。
- 后台订单管理页优化订单卡片排版，增加清晰的订单头、状态徽标、元信息区、商品明细区和地址备注区；状态下拉保存时禁用当前订单并写入 `orders.status`。
- 后台订单管理新增开始日期和结束日期筛选，保留筛选栏单一蓝色“导出订单”入口，按当前搜索、状态和日期范围导出 XLSX，并在文件名中附加日期范围。
- 后台订单管理筛选栏缩短搜索框显示宽度，并统一状态下拉框与日期筛选框的标签和控件对齐。
- 后台订单管理筛选栏调整搜索框和右侧筛选/导出控件在各自网格区域内居中显示。
- 后台订单管理筛选栏改为搜索框、日期、状态和导出按钮紧凑成组居中，避免右侧筛选控件被推到页面最右侧。
- 后台订单管理筛选栏改为占满整行，搜索框自动吃掉剩余宽度，日期、状态和导出订单按钮靠右排列。
- 后台订单管理订单卡片优化视觉层次：头部、状态操作、订单信息、商品明细和地址备注分区更清晰。
- 后台分类管理搜索栏改为占满整行，搜索框铺满工具条宽度。
- 后台分类管理文案调整：编辑区“前台可见”改为“上架”，左侧状态改为“已上架/已下架”。
- 后台分类管理布局优化：左侧分类状态改为紧凑徽标，编辑表单增加标题分隔、字段分组和右对齐操作区，减少大屏留白。
- 后台分类上下架状态变化时增加确认提示，确认后批量同步上下架该分类下的商品。
- 后台删除分类时增加确认提示“该分类下所有商品将同步下架，且分类重置为空”，确认后先下架并清空该分类下商品的分类，再删除分类。
- 后台商品编辑页新增主图和详情图预览管理，可单独移除 URL 引用，也可删除 `product-images` Storage 文件并同步移除 URL。
- 后台商品图片上传压缩最大边长调整为 1200px；前台商品主图和详情图增加最大宽度约束，避免大图撑开详情区域并遮挡购买卡。
- 后台商品图片策略更新为：主图/头图推荐 4:3 且上传后最大宽度 1200px，详情图上传后最大宽度 1000-1200px 且高度不限，商品列表缩略图由前端自动压小显示。


### 文档
- 新增 `PLAN.md`、`TODO.md`、`ARCHITECTURE.md`、`CHANGELOG.md`。
- 新增 `API.md` 接口文档。
- 新增 `.md/openapi.yaml` OpenAPI 规范。
- 后台新增 `/admin/api-docs` Scalar 接口文档页面。
- 服务层方法增加 JSDoc 注释。
- 补充服务方法、参数、OpenAPI 接口参数和核心字段的业务用途说明。
- 新增 Supabase SQL 操作文档 `.md/sql-docs/SUPABASE_SQL_SETUP.md`。
- 新增 Supabase migration：`001_initial_schema.sql`、`002_rls_policies.sql`。
- 将 Supabase 建表 SQL 中的业务时间字段改为北京时间本地时间。
- 新增 Supabase migration：`003_beijing_time_columns.sql`，用于把已存在旧表的时间字段迁移为北京时间。
- `categories.id` 已按当前数据库方案改为 `uuid`，`products.category_id` 同步为 `uuid` 外键。
- `002_rls_policies.sql` 补充 `GRANT` 表访问权限，避免 Supabase REST 在 RLS 判断前返回 403。
- `.md/sql-docs/SUPABASE_SQL_SETUP.md` 新增第 4.1 节表访问权限 SQL 和 `admin_profiles` 登录 403 排查说明。
- 新增 Supabase migration：`006_service_role_order_permissions.sql`，补齐 `submit-order` Edge Function 所需的 `service_role` 商品读取、订单写入和通知配置读取权限。
- `.md/TODO.md` 已按当前自动完成项更新 P0 前台闭环和 P3 来源标记进度。
- 新增 `supabase/seed.sql`，提供 3 个分类和 10 条 Supabase 商品测试数据，可重复执行。
- 重新生成测试商品数据：`public/mock/products.json` 与 `supabase/seed.sql` 同步为 10 条新商品，分类改为固定 UUID，并为商品补充销售规格价格。
- 新增 Supabase migration：`004_product_images_bucket.sql`，用于在 Supabase SQL Editor 创建公开读取、管理员写入的 `product-images` Storage bucket。
- 已在 Supabase 执行 `004_product_images_bucket.sql`，确认 `product-images` bucket 创建成功，单文件大小限制为 2 MB。

### 已知未完成
- 后台商品、分类、订单状态已接 Supabase CRUD；商品图片上传、压缩、预览和 Storage 删除已完成。
- 当前 Supabase Auth 登录还需要在 Supabase 控制台执行最新表权限 SQL，并确认管理员邮箱已写入 `admin_profiles`。
